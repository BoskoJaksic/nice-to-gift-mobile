{
  "routes": [
    {
      "route": "/well-known/assetlinks.json"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/.well-known/*.{json}"]
  },
  "mimeTypes": {
    ".json": "text/json"
  }
}
